'use strict';

angular.module('JaSON')
	.controller('ResponseTabsController',
	[ '$scope', '$log', '$http',
		function ($scope, $log, $http) {

			function init() {
				bindVars();
				bindFunctions();
			};

			function bindVars() {
			}

			function bindFunctions() {
			}

			init();


		}]);
